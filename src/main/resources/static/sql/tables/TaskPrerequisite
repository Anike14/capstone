CREATE TABLE TaskPrerequisites (
    TPID UNIQUEIDENTIFIER NOT NULL DEFAULT (NEWID()),
    TPOWNER UNIQUEIDENTIFIER NOT NULL,
    TaskID UNIQUEIDENTIFIER NOT NULL,
    PRIMARY KEY (TPID),
    FOREIGN KEY (TaskID) REFERENCES Tasks(TID)
);